<Window x:Class="CyberSecurityBot.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberSecurityBot GUI" Height="600" Width="800"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Area -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock x:Name="txtAscii" 
                       FontFamily="Consolas" 
                       Foreground="#00FF00" 
                       FontSize="12" 
                       TextAlignment="Center"/>
            <TextBlock Text="CyberSecurity Awareness Bot" 
                       Foreground="#00D4FF" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Chat Log Area -->
        <Border Grid.Row="1" Background="#252526" CornerRadius="10" Padding="10" Margin="0,0,0,15">
            <ScrollViewer x:Name="scrollChat" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="chatPanel"/>
            </ScrollViewer>
        </Border>

        <!-- Input Area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtInput" 
                     Background="#3C3C3C" 
                     Foreground="White" 
                     Padding="10" 
                     FontSize="14"
                     KeyDown="txtInput_KeyDown"
                     VerticalContentAlignment="Center"/>

            <Button x:Name="btnSend" 
                    Content="Send" 
                    Grid.Column="1" 
                    Width="80" 
                    Margin="10,0,0,0"
                    Background="#00D4FF" 
                    Foreground="Black" 
                    FontWeight="Bold"
                    Click="btnSend_Click"/>
        </Grid>
    </Grid>
</Window>